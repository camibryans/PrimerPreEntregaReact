import 'bootstrap/dist/css/bootstrap.min.css'

const Title = (props) => {
    return (
        <div>
        <h1>{props.title}</h1>
        {/* <h2>{props.subtitle}</h2> */}
        </div>
    )
}

export default Title